Place NIST CTC (Combined Test Case) STEP models here, e.g. from the
NIST MBE PMI Validation and Conformance Testing project (AP242 models).
Files in this directory are gitignored; only paths are referenced by tests.
Suggested minimum set: one single-part model, one small assembly.
